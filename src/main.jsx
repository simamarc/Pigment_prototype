import React from 'react'
import ReactDOM from 'react-dom/client'
import Dashboard from '../pigment-dashboard-wireframe.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Dashboard />
  </React.StrictMode>,
)
